import OS from "../_scripts/OS";

describe("OS", () => {

    // it("OS.title", done =>
    //     expect(OS.title).toBe("MacOS"));
    //
    // it("OS.copyToBufferDefaultHotKeys", done =>
    //     expect(OS.copyToBufferDefaultHotKeys).toBe("Cmd+C"));
});
